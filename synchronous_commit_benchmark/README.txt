What I wanted to test here was whether setting "synchronous_commit = off" would make writes faster.

The result files with the "-async" suffix are the results with benchmark-async.sql while those without the suffix are the results with benchmark.sql.